import Navbar from '../components/Navbar';
import Logo from '../components/Logo';

const Header = () => {
  return (
    <div>
      <Logo />
      <Navbar />
    </div>
  );
};

export default Header;
